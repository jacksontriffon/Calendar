# Calendar

A self-contained calendar app, packaged as a single HTML file (`index.html`).

## Usage

Open `index.html` directly in a browser — no build step or server required.

## Deployment

Every push to `main` deploys via Vercel to https://www.claude-calendar.com.
